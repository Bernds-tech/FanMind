# Supabase Public API configuration (safe for browser usage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_SERVICE_ROLE_KEY is intentionally not required for this MVP auth foundation.
# If it is added later for server-only migrations/admin tasks, never expose it via NEXT_PUBLIC_* or client components.
