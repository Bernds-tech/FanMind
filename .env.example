# Supabase Public API configuration (safe for browser usage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only placeholder for later privileged backend tasks.
# Never expose this value to client components or NEXT_PUBLIC_* variables.
# The current MVP auth foundation does not require it in application code.
SUPABASE_SERVICE_ROLE_KEY=
