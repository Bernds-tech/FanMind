name: One-off referral build diagnostics

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  diagnose:
    if: github.head_ref == 'test/referral-sandbox-lifecycle-20260715'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ github.token }}
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-anon-key
      NEXT_PUBLIC_APP_URL: https://fanmind.ch
      NEXT_PUBLIC_SITE_URL: https://fanmind.ch
      FANMIND_PUBLIC_DEMO_ENABLED: "false"
      FANMIND_ENABLE_REFERRAL_BILLING: "false"
      FANMIND_ENABLE_TELEGRAM_SEND: "false"
      FANMIND_ADMIN_EMAILS: admin@example.com
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run production build and capture output
        id: build
        shell: bash
        run: |
          set +e
          npm run build > /tmp/referral-build.log 2>&1
          status=$?
          cat /tmp/referral-build.log
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Report build result
        shell: bash
        run: |
          {
            if [ "${{ steps.build.outputs.status }}" = "0" ]; then
              echo '## Referral-Build erfolgreich'
            else
              echo '## Referral-Build fehlgeschlagen'
            fi
            echo
            echo '```text'
            tail -n 200 /tmp/referral-build.log
            echo '```'
          } > /tmp/referral-build-comment.md
          gh pr comment 571 --repo "$GITHUB_REPOSITORY" --body-file /tmp/referral-build-comment.md

      - name: Fail when build failed
        if: steps.build.outputs.status != '0'
        run: exit 1
