name: One-off central AI tier policy 20260718

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write
  pull-requests: write

jobs:
  apply:
    if: github.head_ref == 'feat/central-ai-tier-policy-20260718' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/central-ai-tier-policy-20260718
          fetch-depth: 0
          persist-credentials: true

      - name: Apply central AI tier policy
        id: apply
        shell: bash
        run: |
          set +e
          python3 scripts/one-off/apply-central-ai-tier-policy-20260718.py > /tmp/ai-tier-policy.log 2>&1
          status=$?
          cat /tmp/ai-tier-policy.log
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Report failure
        if: steps.apply.outputs.status != '0'
        shell: bash
        run: |
          {
            echo '## Central AI tier integration failed'
            echo
            echo '```text'
            tail -n 200 /tmp/ai-tier-policy.log
            echo '```'
          } > /tmp/ai-tier-policy-comment.md
          gh pr comment "$GITHUB_EVENT_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file /tmp/ai-tier-policy-comment.md

      - name: Stop after failure
        if: steps.apply.outputs.status != '0'
        run: exit 1

      - name: Validate patch
        run: git diff --check

      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/app/settings/AccountSections.tsx \
            package.json \
            scripts/verify-product-truth.mjs \
            docs/SOURCE_OF_TRUTH.md \
            README.md \
            AGENTS.md
          if git diff --cached --quiet; then
            echo "No generated changes."
            exit 0
          fi
          git commit -m "feat: integrate central AI tier policy"
          git push origin HEAD:feat/central-ai-tier-policy-20260718
